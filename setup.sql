create table normal (
	date timestamp,
	remark text,
	credit varchar(255),
	balance varchar(255),
	ref varchar(255)
);

create table zstd (
	date timestamp,
	remark text,
	credit varchar(255),
	balance varchar(255),
	ref varchar(255)
);
